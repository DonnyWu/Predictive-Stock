"""stockrater — a simple, transparent stock rating engine.

All scoring logic lives in this package and has no UI dependencies, so it can be
unit-tested in isolation and reused by any frontend (the Streamlit ``app.py``).
"""

__version__ = "0.1.0"
